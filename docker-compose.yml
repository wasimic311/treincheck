services:
  db:
    image: postgres:16
    environment:
      POSTGRES_USER: treincheck
      POSTGRES_PASSWORD: localdev
      POSTGRES_DB: treincheck
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
